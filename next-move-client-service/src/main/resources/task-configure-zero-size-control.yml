taskConfig:
  tasks:
#=========== Task 07 (CTRL): viewfs -> direct s3a
    - id: task-07
      type: ZERO_SIZE_CONTROL
      sourceType: HADOOP
      rootPath:
        source: /src-local-02/main/submain/movethis2/z*-1
        #        source: /src-local-02/main/submain/mov*/zer*
        #        destination: /minio
        destination: s3a://sftp/direct/zero-size
      filesPerRound: 100
      #      afterDate: '12/08/2024 08:00:00'
      #      beforeCurrentDateInHours: 5
      #      beforeCurrentDateInDays: 3
      srcExtensions:
        - txt
        #        - txt.gz
        #        - tar.gz
        #        - txt
        #        - zip
        - Z
      #        - zip
      wildcardPatterns:
        - test*-1
      #        - data-1*
      ctrlExtensions:
        - ctrl
      compression:
        notExtract: false
        createTargetDirectory: false
      overwrite: false
      checkFileSize: true
      checkFileDelaySeconds: 10
      moveType: COPY
      retry: 1
      cronExpression: "0/2 * * ? * * *"

#=========== Task 08(CTRL) :  sftp -> direct s3a
    - id: task-08
      type: ZERO_SIZE_CONTROL
      sourceType: SFTP
      rootPath:
        source: /home/blendata/test_cp/src/sf*
        destination: s3a://sftp/direct/zero-size/from-sftp
      sourceProperties:
        host: "49.0.196.113"
        port: 22
        username: "blendata"
        privateKeyPath: "/Users/anucha.r/Documents/Document-ME/KeyPair-DEV.cer"
        connectionTimeout: 30000
        readTimeout: 30000
      filesPerRound: 100
      #      afterDate: '12/08/2024 08:00:00'
      #      beforeCurrentDateInHours: 5
      #      beforeCurrentDateInDays: 3
      srcExtensions:
        - csv
      ctrlExtensions:
        - ctrl
      compression:
        notExtract: false
        createTargetDirectory: false
      overwrite: false
      checkFileSize: true
      checkFileDelaySeconds: 10
      moveType: COPY
      retry: 1
      cronExpression: "0/2 * * ? * * *"